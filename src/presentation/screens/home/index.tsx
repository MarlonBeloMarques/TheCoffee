import React from 'react';
import { ScrollView, View } from 'react-native';

const Home: React.FC = () => {
  return (
    <View>
      <ScrollView testID="options_list_id" />
    </View>
  );
};

export default Home;
